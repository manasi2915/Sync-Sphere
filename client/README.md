# SyncSphere — Rebuilt (Landing + Pages)

This rebuild follows your screenshots and uses React + Tailwind (JSX). It includes:
- Landing page at `/` (public, pre-login)
- Login page at `/login`
- Protected Dashboard at `/dashboard` with top nav linking Events, Study, Resources, Chat, Expenses, Admin
- Events & Resources UIs matching requested actions (Register, Cancel, Buy, Reserve)
- Admin panel styled like your screenshot (tabs, resources list, analytics)

Run:
1. npm install
2. npm run dev
